# HH.ru Auto Responder 🚀

![Version](https://img.shields.io/badge/version-2.1.2-blue.svg)
![Platform](https://img.shields.io/badge/platform-Tampermonkey-green.svg)
![Status](https://img.shields.io/badge/status-stable-success.svg)

**UserScript для автоматизации откликов на HeadHunter (hh.ru).**
Экономит время при массовых откликах, сохраняет вакансии с вопросами и устойчиво работает с редиректами.

---

## ✨ Возможности

### 🧠 Надёжная работа с формами и UI

* Корректно взаимодействует с React / Magritte UI (HH).
* Корректно заполняет поля через нативный setter `HTMLTextAreaElement.prototype.value` и триггерит `input`.
* Поддерживает режимы с сопроводительным письмом и без него.
* Автоматически подтверждает модальные окна вроде «Готовность к переезду».

### 🔁 Стабильность, защита от редиректов и восстановление

* Watchdog следит за URL и состоянием процесса.
* При редиректе на тесты/анкеты скрипт помечает вакансию как обработанную, возвращается назад и при необходимости обновляет список.
* Есть механизмы защиты от зацикливания при редиректах (trap lock) и автосброса этой метки.
* Скрипт может автоматически возобновить работу после возврата со страницы вопросов.

### 💾 Сохранение вакансий с вопросами

* Автоматическое сохранение вакансий, требующих ответов на вопросы
* Формируется отдельный список для ручного отклика
* Экспорт списка в HTML (фильтры + режимы "Новые/Открытые")
* Прямые ссылки на страницы с вопросами для последующего прохождения

### 🔒 Кросс-вкладочная безопасность

* Instance lock в `localStorage` предотвращает одновременную работу в нескольких вкладках.
* Lock поддерживает TTL и регулярно обновляет timestamp, чтобы другие вкладки видели, что процесс жив.

### 🤖 "Человеческое" поведение

* Имитация просмотра вакансии: плавный скролл до 60% страницы, паузы и возврат.
* Рандомизированные задержки между действиями.
* Ограничение числа откликов за запуск.

### 🔎 Инструменты и диагностика

* UI-панель с настройками и логом.
* Управление сохранёнными вакансиями с вопросами
* Кнопки Start / Stop, Healthcheck, Reset history.
* Healthcheck проверяет ключевые селекторы и статус instance lock.

---

## 📥 Установка скрипта

### 1. Установка Tampermonkey

Установи Tampermonkey для браузера:

* Chrome / Edge — Chrome Web Store
* Firefox — Firefox Add-ons
* Safari — App Store (если доступно)

---

### 2. Первичная настройка Tampermonkey (важно)

Проверь в Dashboard Tampermonkey, что скрипт включён и Tampermonkey разрешён для hh.ru. В Chromium можно временно дать Site access → On all sites для теста.

---

## Общие требования

1. Tampermonkey установлен из официального магазина.
2. Скрипт сохранён в Dashboard и включён.
3. При изменениях перезагрузи вкладку hh.ru.

---

## Быстрая проверка работоспособности

1. Открой `https://hh.ru/search/vacancy`.
2. Должна появиться панель **HH AutoResponder**.
3. Если панели нет: проверь права расширения, включён ли скрипт в Dashboard, перезагрузи страницу.

---

### 3. Добавление скрипта

1. Открой Tampermonkey → Dashboard.
2. Create a new script → вставь код → Сохрани (`Ctrl + S`).
3. Убедись, что скрипт включён.

---

## ▶️ Использование

### Основной функционал
1. Перейди на страницу поиска вакансий: `https://hh.ru/search/vacancy`.
2. Настрой: сопроводительное письмо, задержки, лимит, время чтения вакансии.
3. Нажми **START**. Скрипт пройдет по списку и отправит отклики.
4. Если нужно остановить — нажми **STOP**. Скрипт аккуратно снимет флаги и освободит instance lock.

### Работа с вакансиями с вопросами
* При попадании на вакансию с тестами/вопросами, она автоматически сохраняется в раздел **"Сохранённые для ручного отклика"**
* Используйте кнопку **Export** для сохранения списка в HTML-файл
* **Open** — открывает страницу с вопросами в новой вкладке
* **Remove** — удаляет вакансию из списка
* **Clear** — полностью очищает список
* В экспортном HTML есть режимы **Новые / Открытые**: открытые скрываются в "Новых", но доступны отдельно

Рекомендуемые задержки: от 1000 мс. Практически удобные значения 1500–3500 мс. Не ставь слишком маленькие значения.

---

## 🔄 Поведение при тестах и вопросах

Если hh.ru переадресует на страницу с тестом/вопросами (`/applicant/vacancy_response`), скрипт:

1. **Сохраняет вакансию** в список для ручного отклика
2. Пометит вакансию как обработанную (чтобы не зациклиться).
3. Попробует вернуться назад (history.go(-2) / history.back).
4. Если возвращение не сработало — перейдёт по сохранённому URL списка и выполнит F5.
5. После обновления может автоматически возобновить работу.

**Важно**: Вакансии с вопросами сохраняются, чтобы вы могли вернуться к ним позже и пройти тестирование вручную.

---

## 🛠 Технические детали

* История обработанных вакансий хранится в `sessionStorage`.
* Instance lock — в `localStorage` с TTL (по умолчанию ~30 с). TabId сохраняется в lock.
* **Новое в v2.1.0**: Список вакансий с вопросами хранится в `localStorage` под ключом `manual_list`
* **Новое в v2.1.2**: HTML-экспорт поддерживает режимы "Новые/Открытые" и скрывает открытые из списка "Новые"
* Trap lock — временная метка в `sessionStorage`, автоочистка через 15 с.
* Watchdog реализован через `setInterval`, ожидание элементов — через `MutationObserver`.
* Заполнение textarea идёт через нативный setter + событие `input`.
* Генерация уникального ID вакансии: попытка взять id из ссылки, fallback на хеш FNV-1a от содержимого карточки.
* Скрипт устойчив к динамическим изменениям DOM и редиректам.

---

## ⚠️ Рекомендации

* Не ставь задержки ниже 1000 мс.
* Не указывай слишком большой лимит за один запуск.
* Избегай одновременного запуска в нескольких вкладках.
* Используй Healthcheck перед массовой рассылкой, чтобы убедиться, что селекторы на месте.
* **Для вакансий с вопросами**: периодически экспортируй список, чтобы не потерять ссылки.

---

## ⚠️ Disclaimer

Инструмент предназначен только для личного использования. Автор не несёт ответственности за возможные ограничения или блокировки со стороны hh.ru при агрессивном использовании. Используй скрипт разумно.
