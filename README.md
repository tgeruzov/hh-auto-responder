# HH.ru Auto Responder 🚀

![Version](https://img.shields.io/badge/version-1.1-blue.svg)
![Platform](https://img.shields.io/badge/platform-Tampermonkey-green.svg)
![Status](https://img.shields.io/badge/status-stable-success.svg)

**UserScript для автоматизации откликов на HeadHunter (hh.ru).**
Скрипт экономит время при массовом отклике на вакансии и снижает рутинную нагрузку при активном поиске работы.

---

## ✨ Возможности

### 🧠 Умная обработка форм

* Корректно работает с React / Magritte UI (HH).
* Одинаково стабильно работает:

  * **с сопроводительным письмом**
  * **без сопроводительного письма**
* Автоматически подтверждает модальное окно **«Готовность к переезду»**.

### 🔁 Стабильность и восстановление

* **Watchdog-механизм** — постоянно следит за URL и состоянием процесса.
* **Авто-F5**:

  * При редиректе на тесты или вопросы
  * Скрипт сам возвращается назад
  * Выполняет принудительную перезагрузку страницы
* **Авто-возобновление**:

  * После возврата со страницы тестов
  * Без повторного нажатия кнопки START

### 🛡 Безопасность

* Рандомизированные задержки между действиями.
* История обработанных вакансий хранится в `sessionStorage`.
* За одну сессию повторные отклики исключены.

---

## 📥 Установка скрипта

### 1. Установка Tampermonkey

Установите расширение Tampermonkey для вашего браузера:

* [Chrome / Edge](https://chrome.google.com/webstore/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)
* [Firefox](https://addons.mozilla.org/firefox/addon/tampermonkey/)
* [Safari](https://apps.apple.com/us/app/tampermonkey/id1482490089)

---

### 2. Первичная настройка Tampermonkey (ОБЯЗАТЕЛЬНО)

Перед первым запуском скрипта нужно убедиться, что браузер и расширение Tampermonkey действительно разрешают выполнение пользовательских скриптов на сайтах (включая hh.ru). Ниже — проверенные шаги для трёх основных браузеров.

---

## Общие требования (вне зависимости от браузера)

1. Установи Tampermonkey из официального магазина расширений (Chrome Web Store / Firefox Add-ons / App Store для Safari).
2. В панели Tampermonkey → **Dashboard** проверь, что скрипт сохранён и переключатель рядом с ним включён (ON).
3. Если скрипт не запускается — перезагрузи вкладку hh.ru после изменений настроек расширения.

---

## Chrome / Edge (Chromium)

1. Открой страницу управления расширениями:

   * Chrome: `chrome://extensions/`
   * Edge: `edge://extensions/`
2. Найди **Tampermonkey** и нажми **Details / Подробности**.
3. Убедись, что включены следующие опции:

   * **Enabled / Включено** — расширение активно.
   * **Allow access to file URLs** — если планируешь использовать локальные скрипты (необязательно для обычной установки).
   * **Site access / Доступ к сайтам** — выбери либо **On all sites (рекомендуется для тестирования)**, либо **On click** и затем явно разреши Tampermonkey для `hh.ru`, когда появится запрос.
4. Если в интерфейсе есть переключатель **Allow user scripts** — включи его (если он отображается).
5. Перейди на hh.ru и при необходимости разреши выполнение скрипта по подсказке Tampermonkey (если браузер/расширение запросит разрешение для текущего сайта).

**Совет.** Для простоты на этапе проверки можно поставить Site access → *On all sites*, а после убедиться в корректной работе — переключить на более строгий режим.

---

## Firefox

1. Открой страницу дополнений: `about:addons`.
2. Найди **Tampermonkey** → кликни на три точки → **Manage** (Управление).
3. Убедись, что расширение **включено**.
4. Если используешь приватные окна — при необходимости включи опцию **Run in Private Windows**.
5. Перейди на hh.ru. Если Tampermonkey не запустил скрипт автоматически:

   * Нажми на иконку Tampermonkey в панели инструментов;
   * В списке скриптов проверь, что нужный скрипт **включён** и разрешён для текущего сайта (`hh.ru`).
   * При первом обращении расширение может показать запрос на разрешение доступа к сайту — подтвердите.

---

## Safari (macOS / iOS — если поддерживается)

> Важное замечание: архитектура расширений в Safari отличается от Chromium/Firefox. Tampermonkey для Safari поставляется как App Store-приложение и требует явного включения в настройках Safari.

1. Установи Tampermonkey из **App Store** (или используемый тобой официальный источник расширения).
2. Открой **Safari → Settings (Preferences) → Extensions (Расширения)**.
3. Включи Tampermonkey (галочка рядом с расширением).
4. Параметры доступа:

   * В настройках расширения в Safari выбери, для каких сайтов разрешён запуск — укажи **All websites** или добавь `hh.ru`.
5. Открой Tampermonkey (если приложение/панель есть) → Dashboard → проверь, что скрипт **включён**.
6. Перезагрузи страницу hh.ru и проверь, появился ли UI скрипта.

**Особенности Safari:**

* На macOS иногда нужно перезапустить Safari / дать дополнительные разрешения в System Settings.
* Мобильная версия Safari (iOS) поддерживает расширения только в последних версиях iOS; опыт может отличаться и некоторые функции Tampermonkey могут быть ограничены.

---

## Быстрая проверка работоспособности

1. Открой `https://hh.ru/search/vacancy` в том же браузере, где включён Tampermonkey.
2. Подожди ~2–3 секунды — на странице должна появиться панель **HH AutoResponder** в правом нижнем углу.
3. Если панели нет:

   * Открой Tampermonkey Dashboard → убедись, что скрипт включён.
   * Проверь ограничения доступа расширения к сайтам (Site access / Permissions).
   * Перезагрузи вкладку/браузер.

---

### 3. Добавление скрипта

1. Откройте **Tampermonkey → Dashboard**
2. Нажмите **“Create a new script”**
3. Удалите шаблонный код
4. Вставьте **полный код скрипта** из репозитория
5. Сохраните (`Ctrl + S`)
6. Убедитесь, что скрипт **включён** (тумблер ON)

---

## ▶️ Использование

1. Перейдите на страницу поиска вакансий:
   👉 [https://hh.ru/search/vacancy](https://hh.ru/search/vacancy)
2. В правом нижнем углу появится панель **HH AutoResponder**

   * Если панель скрыта — нажмите на иконку **🤖** справа
3. Настройте параметры:

   * **Сопроводительное письмо**

     * Можно включить или полностью отключить
     * Скрипт работает одинаково стабильно в обоих режимах
   * **Задержка (Min / Max)**

     * Рекомендуется: `1500–3500` мс
   * **Лимит**

     * Максимальное количество откликов за запуск
4. Нажмите **START**

---

## 🔄 Поведение при тестах и вопросах

* Если HH перенаправляет на:

  * тест
  * анкету
  * страницу `/applicant/vacancy_response`
* Скрипт:

  1. Зафиксирует состояние
  2. Вернётся назад
  3. Выполнит принудительный F5
  4. Автоматически продолжит работу

❗ **Ничего нажимать вручную не нужно**

---

## 🛠 Технические детали

* Используется `sessionStorage` для:

  * истории вакансий
  * состояния выполнения
  * восстановления после перезагрузки
* Реализован Watchdog через `setInterval`
* Для ввода текста используется нативный setter `HTMLTextAreaElement.prototype.value`
* DOM-ожидания реализованы через polling, а не `MutationObserver` для стабильности

---

## ⚠️ Рекомендации

* Не ставьте задержки ниже `1000 мс`
* Не используйте слишком большой лимит за один запуск
* Не запускайте параллельно несколько вкладок с активным скриптом

---

## ⚠️ Disclaimer

Инструмент предназначен **только для личного использования**.
Автор не несёт ответственности за возможные ограничения или блокировки аккаунта со стороны hh.ru при агрессивном использовании. Используйте скрипт разумно.
